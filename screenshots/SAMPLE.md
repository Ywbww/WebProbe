# Sample Output

The `.html` and `.txt` files in this folder are sample WebProbe output
against `http://example.com` — included so you can see the report
format without running the tool yourself.

For real audits, run WebProbe against your own target and the files
land in the project root.
